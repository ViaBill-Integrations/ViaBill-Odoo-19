# Migration 19.0.4.0.0
# Adds the viabill.refund.wizard transient model (no schema changes needed for
# TransientModel; Odoo creates the table automatically on upgrade).
# Also adds the security/ir.model.access.csv file.


def migrate(cr, version):
    pass
